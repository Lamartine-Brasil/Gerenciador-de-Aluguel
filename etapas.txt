# STATUS DO PROJETO - Sistema de Gerenciamento de Aluguéis

Este arquivo reflete o estado ATUAL do projeto (não é um histórico de fases nem um changelog
por data). Ao implementar algo novo, atualize a seção correspondente aqui e em README.md.

## Mapa das telas do sistema

A navegação fica numa sidebar vertical fixa (280px, recolhível para 76px, vira gaveta no
celular) com as 11 telas agrupadas em 5 blocos rotulados:

  Dashboard
  GESTÃO      │ Imóveis, Contratos, Atrasos, Histórico
  FINANCEIRO  │ Despesas, Gráficos, Relatórios
  AGENDA      │ Calendário
  SISTEMA     │ Auditoria, Configurações

As seções abaixo seguem a ordem antiga de leitura (Dashboard, cadastros, financeiro,
análises, sistema), que continua útil para navegar este documento. "Pagamentos" não é uma
tela — é o modal de registrar pagamento, acessado de dentro de Contratos ou Atrasos — por
isso aparece logo depois de Contratos.

## ✅ IMPLEMENTADO E FUNCIONANDO

### Login e conta
- [x] Tela de login (usuário + senha)
- [x] Autenticação validada no servidor (`api/login.php`)
- [x] Sessão via cookie assinado (HMAC-SHA256), 30 dias, HttpOnly + SameSite=Lax — não usa
      `session_start()` do PHP, não guarda nada de sessão no servidor
- [x] Permanece logado ao trocar de aba/página ou reabrir o navegador
- [x] Botão "Sair" limpa a sessão
- [x] Troca de usuário/senha pela tela de Configurações (exige senha atual)
- [x] Usuário/senha padrão: `admin` / `12345678` (só na primeira execução)
- [x] Senha sempre salva com `password_hash` (nunca texto puro), mínimo 8 caracteres em
      qualquer senha nova (própria ou de outro usuário) — antes era só 4
- [x] Bloqueio por tentativas de login (`api/login.php`): 5 tentativas erradas seguidas
      do mesmo IP bloqueiam novas tentativas (mesmo com a senha certa) por 15 minutos.
      Guardado em `data/login_attempts.json` (protegido pelo mesmo `.htaccess` de
      `data/`, e fora do controle de versão — é só um contador temporário)
- [x] Múltiplos usuários administradores, todos com as mesmas permissões (acesso total).
      Em Configurações: listar, adicionar e remover outros usuários — as duas ações
      exigem confirmar a própria senha atual (é uma ação de alto impacto: dá acesso
      total a alguém novo, ou tira o acesso de alguém). Não é possível remover a si
      mesmo nem remover o último usuário restante. `data/auth.json` antigo
      (usuário único) é migrado automaticamente para o novo formato com lista de
      usuários no primeiro acesso, sem exigir nenhuma ação manual

### Visão Geral — Dashboard
- [x] Card: total de dívidas ativas (não pagas e dentro do prazo)
- [x] Card: total em atraso (R$) — soma só o componente de juros/multa (resumo rápido,
      diferente da convenção "valor cheio" usada em Atrasos/Relatórios — decisão consciente)
- [x] Card: próximo vencimento
- [x] Card: total de despesas lançadas no mês atual
- [x] Alerta de dívidas vencendo nos próximos 5 dias e alerta de contratos no "aniversário"
      de reajuste (1 ano desde o último reajuste, ou desde o início se nunca reajustado, com
      o valor sugerido) — os dois alertas têm cores diferentes (vencimento em laranja,
      reajuste em azul) para não se confundirem visualmente, e cada item dentro deles é
      clicável: no de vencimento, leva direto para o contrato na aba Contratos (busca
      automaticamente por "#número"); no de reajuste, abre direto o modal "Reajustar" já
      com o valor sugerido preenchido
- [x] Lista das dívidas mais recentes

### Cadastros — Imóveis
- [x] Aba dedicada de cadastro de imóveis (descrição livre, ex: "Apartamento 302 - Rua
      das Flores #123"), gerenciada como uma lista simples (adicionar/editar/remover)
- [x] Editar o nome/descrição de um imóvel já cadastrado atualiza também todos os
      contratos que já usavam a descrição antiga (diferente de remover, que não afeta
      contratos existentes) — pensado para corrigir erros de digitação sem deixar
      contratos antigos "órfãos" com um nome que não existe mais na lista
- [x] Ao criar ou editar um contrato, o imóvel é escolhido de um seletor alimentado por
      essa lista, em vez de digitado como texto livre — evita erros de digitação e
      duplicidade. Se não houver nenhum imóvel cadastrado ainda, um aviso no formulário
      de novo contrato indica para cadastrar um na aba Imóveis primeiro
- [x] Remover um imóvel da lista não afeta contratos que já usam aquela descrição

### Cadastros — Contratos
- [x] Modelo de dados: um **contrato** (imóvel + inquilino, dados compartilhados) guarda
      dentro de si uma lista de **dívidas** (uma por mês/ciclo de cobrança: vencimento,
      valores, se foi paga, pagamentos) — em vez de um contrato = um vencimento só. Um
      contrato antigo lançado retroativamente vira 1 contrato com N dívidas em aberto,
      não N contratos separados (bug real relatado e corrigido nessa reestruturação).
      Migração automática e transparente do formato antigo, sem perder nenhum dado,
      testada com dados reais de produção antes de entrar no ar
- [x] Listar, criar, editar, excluir — 1 card por contrato, com a lista de dívidas
      daquele contrato dentro do card (cada dívida com seu próprio status, valores e
      ações de pagar/editar/excluir)
- [x] Número sequencial único gerado automaticamente na criação (`#1`, `#2`...), exibido
      no card do contrato e nos modais de pagamento/histórico, usado só para identificar
      o contrato (não tem relação com o `id` interno). Nunca se repete, mesmo depois de
      excluir contratos. Também entra na busca (buscar por "#12" encontra o contrato 12).
      Contratos de instalações antigas (sem número) recebem um automaticamente na
      primeira vez que os dados são carregados, na ordem de criação
- [x] Campos do contrato (compartilhados entre as dívidas): imóvel, inquilino, quem
      recebe (padrão), data de início, dia de pagamento, anexo do contrato assinado,
      caução (opcional)
- [x] Campos de cada dívida: vencimento, aluguel, desconto, juros, multa, condomínio,
      valor em atraso, observação — total calculado automaticamente
- [x] Busca por inquilino, imóvel ou número do contrato (`#12`); filtro por ano, mês e
      status (um contrato aparece se pelo menos uma das suas dívidas bater com o filtro)
- [x] Ao criar um contrato novo, os campos Juros e Multa são digitados em percentual
      (%) do aluguel, não mais em R$ — vêm pré-preenchidos com a taxa de juros/multa
      configurada em Configurações (a mesma usada no cálculo automático de atraso) e
      são convertidos para R$ ao salvar a dívida. Na edição de uma dívida já existente,
      continuam sendo digitados direto em R$ (valor fixo daquele mês, sem recalcular)
- [x] Criação de contrato pede "Data de início do contrato" e "Dia de pagamento" (1-31)
      — o primeiro vencimento é calculado a partir desses dois campos (com ajuste
      automático se o dia não existir naquele mês, ex: dia 31 em fevereiro vira o
      último dia do mês)
- [x] Lançamento retroativo de contratos antigos: se a data de início informada já
      passou, o sistema gera automaticamente uma dívida para cada mês seguinte (mesmo
      dia de pagamento, com ajuste automático em meses menores) até o mês atual, todas
      dentro do MESMO contrato — evita ter que cadastrar manualmente cada mês em atraso.
      Pede confirmação antes, mostrando quantas dívidas serão criadas. O "valor em
      atraso já existente" só é aplicado na dívida mais recente gerada, não em todas
- [x] Botão "Atualizar dívidas" em cada contrato: gera as dívidas que faltam entre a
      última já existente e o mês atual, usando os valores padrão atuais do contrato —
      é assim que um contrato "cresce" mês a mês conforme o tempo passa. Botão global no
      topo do sistema faz isso de uma vez para todos os contratos, e a mesma checagem
      roda sozinha, silenciosamente, toda vez que o sistema é aberto (login) — garante
      que as dívidas ficam sempre em dia sem depender de cron no servidor nem de o
      usuário lembrar de clicar em nada
- [x] Editar contrato (dados compartilhados: imóvel, inquilino, quem recebe, caução,
      corretor, anexo) é uma tela separada de editar uma dívida específica (vencimento e
      valores daquele mês)
- [x] Lista paginada (20 contratos por página), com botões Anterior/Próxima e indicador
      de página. A página volta para 1 automaticamente ao mudar busca ou filtros
- [x] Anexar o contrato assinado (PDF, JPG ou PNG, até 15MB) — um por contrato (não por
      dívida), pela tela "Editar contrato". Arquivos ficam em `contratos/` (pasta
      protegida por `.htaccess`, como `data/`), renomeados automaticamente no servidor
      para `nomedoinquilino-imovel-idcurto.ext` (removendo acentos e caracteres
      especiais). Só é possível anexar em um contrato já existente
- [x] Reajustar valor: botão "Reajustar" no contrato lança um novo valor de aluguel —
      atualiza o valor padrão do contrato (usado em dívidas futuras) e também todas as
      dívidas ainda não pagas; dívidas já pagas mantêm o valor histórico. Fica
      registrado na Auditoria com o valor antigo, o novo e quantas dívidas foram
      atualizadas. Qualquer usuário administrador pode reajustar (decisão consciente:
      sem restrição de usuário específico)
- [x] Excluir contrato apaga o contrato inteiro e todas as suas dívidas (com aviso
      mostrando quantas); excluir uma dívida específica remove só aquele mês, mantendo
      o resto do contrato
- [x] Corretor (opcional, por contrato): nome (escolhido da lista de "Pessoas"
      cadastrada em Configurações, para não digitar toda vez) + percentual (padrão
      configurável em Configurações, ex: 5%). O valor do corretor é só um cálculo
      informativo mostrado em cada dívida (aluguel da dívida × percentual) — mesma
      lógica do condomínio: faz parte do que passa pela mão do proprietário, mas não
      conta como receita dele. Não é somado ao total cobrado do inquilino, não muda
      nenhum vencimento, e não tem rastreio de pagamento separado. O campo de percentual
      só aparece quando um corretor é de fato escolhido (com "Nenhum" selecionado, a
      comissão simplesmente não existe); ao trocar de "Nenhum" para um corretor, o
      percentual volta ao padrão configurado
- [x] Caução (opcional, por contrato): valor digitado na criação ou em "Editar
      contrato". É só uma anotação informativa exibida no card do contrato — nunca
      soma em nenhum total, dívida, atraso ou relatório, já que em teoria é devolvida
      ao inquilino no final
- [x] Devolução de caução: botão "Devolver caução" (visível quando o contrato tem
      caução) abre um modal para registrar data, valor devolvido e observação — só um
      registro informativo, não soma nem subtrai de nenhum total. Depois de registrada,
      o botão vira "Editar devolução da caução" para corrigir data/valor se necessário.
      Fica registrado na Auditoria (`caucao_devolvida`)
- [x] Reajuste sugerido: cada contrato tem um "aniversário" de reajuste — 1 ano após o
      último reajuste aplicado (`dataUltimoReajuste`, atualizado toda vez que "Reajustar"
      é usado), ou após a data de início se nunca foi reajustado. Contratos ativos que já
      passaram do aniversário mostram um aviso no card e no Dashboard, com um valor de
      aluguel sugerido calculado a partir do "Percentual de reajuste sugerido" configurável
      em Configurações (não há consulta a nenhum índice externo real como IGP-M/IPCA — o
      sistema não tem acesso à internet; o percentual é só uma estimativa ajustada
      manualmente). Ao abrir "Reajustar" num contrato nessa situação, o valor sugerido já
      vem pré-preenchido, com uma explicação do cálculo
- [x] Encerrar contrato: botão "Encerrar contrato" marca o contrato como encerrado (com
      confirmação) — diferente de excluir, NÃO apaga nada: todas as dívidas e o
      histórico de pagamentos continuam existindo e visíveis normalmente, só faz o
      sistema parar de gerar novas dívidas mensais automaticamente (nem pelo botão
      "Atualizar dívidas" nem pela atualização automática de login). Pensado para
      quando um inquilino deixa o imóvel, mantendo o histórico do contrato. Mostra um
      badge "Encerrado" e a data no card; pode ser revertido a qualquer momento pelo
      botão "Reabrir contrato"

### Cadastros — Pagamentos (modal, acessado de Contratos/Atrasos)
- [x] Modal "Registrar pagamento" (por dívida) com: data, valor pago, desconto, forma
      de pagamento, quem recebeu, observação
- [x] Valor sugerido automaticamente = total da dívida + atraso atual (juros/multa)
- [x] Campo "Desconto (R$)": ao digitar, recalcula o valor pago automaticamente
      (total + atraso - desconto); o campo valor pago continua editável manualmente depois
- [x] Campo "Motivo do desconto": aparece só quando o desconto é maior que zero — campo
      dedicado para registrar o porquê do desconto, separado da "Observação" genérica.
      Exibido no Histórico (aba geral, modal por contrato e exportação CSV) junto do
      valor do desconto
- [x] Campo "Forma de pagamento": seleção obrigatória entre Dinheiro e Pix
- [x] Campo "Quem recebeu": seletor da lista de "Pessoas" cadastrada em Configurações
      (a mesma lista usada para corretor), pré-selecionado com o recebedor padrão do
      contrato — evita digitar toda vez. Não é mais obrigatório
- [x] Ao confirmar, marca a dívida como paga, registra a data do pagamento e zera o
      atraso base daquela dívida (as outras dívidas do mesmo contrato não são afetadas)

### Financeiro — Atrasos
- [x] Aba dedicada listando só as dívidas com status "atrasado" (de todos os contratos)
- [x] Card com o total em atraso (aluguel + juros/multa) somando todas as dívidas
      atrasadas — valor cheio devido, diferente do card do Dashboard que soma só o
      componente de juros/multa
- [x] Cálculo automático de juros/multa conforme a taxa configurada

### Financeiro — Histórico de pagamentos
- [x] Aba geral com todos os pagamentos de todas as dívidas de todos os contratos,
      filtrável por contrato
- [x] Modal de histórico por contrato (acessível pelo botão "Histórico"), somando os
      pagamentos de todas as dívidas daquele contrato, mostrando de qual dívida
      (vencimento) veio cada pagamento
- [x] Exibição de desconto e forma de pagamento em cada registro do histórico
- [x] Exportação CSV do histórico de um contrato específico (inclui desconto, forma de
      pagamento e valor líquido)
- [x] "Valor líquido" exibido ao lado do "Valor pago" (na aba geral e no modal por
      contrato) sempre que houver corretor e/ou condomínio na dívida — desconta a
      comissão do corretor (% sobre o aluguel) e o valor do condomínio do pagamento,
      já que nenhum dos dois fica com o proprietário (mesma lógica usada nas dívidas:
      "passa pela mão, mas não é receita")

### Financeiro — Despesas
- [x] Aba dedicada para lançar despesas simples (data, descrição, valor), com
      formulário sempre visível no topo (cadastro rápido, pensado para lançar várias
      despesas seguidas)
- [x] Cada despesa pode opcionalmente ser vinculada a um contrato (seletor "Contrato
      relacionado") ou ficar como despesa geral (não afeta cálculo de dívida/receita —
      é só um registro de controle à parte)
- [x] Filtro por ano e por mês; card com total do período filtrado e card com total do
      ano inteiro (independente do mês filtrado), para consultar tanto mensal quanto
      anualmente
- [x] Lista das despesas do período filtrado, mais recente primeiro, com botão editar
      (reaproveita o mesmo formulário do topo, trocando para modo "Editar despesa" com
      botão "Cancelar edição") e botão excluir por item
- [x] Despesas também aparecem fora da própria aba: card "Despesas no mês" no Dashboard,
      coluna "Despesas" e card "Total de despesas no ano"/"Lucro líquido no ano" em
      Relatórios, e gráfico "Despesas mensais" em Gráficos (ver abaixo)

### Análises — Gráficos
- [x] Donut "Contratos por status" (ativos/atrasados/pagos), com legenda e total no centro
- [x] Donut "Pagamentos por forma (líquido)" (Dinheiro/Pix/Não informado), por valor
      líquido recebido — já descontando corretor e condomínio, mesma convenção de
      Relatórios (usa `valorLiquidoPagamento()`)
- [x] Linha "Evolução do total em atraso" (últimos 6 meses), com gridlines e área
      preenchida — continua com o valor cheio devido (não é receita, é dívida em aberto)
- [x] Linha "Receita mensal líquida" (últimos 6 meses, valor líquido efetivamente
      recebido, já descontando corretor e condomínio)
- [x] Linha "Despesas mensais" (últimos 6 meses, mesmo estilo da receita mensal)
- [x] Barras horizontais "Inadimplência — quem mais atrasa": ranking (top 6) por total em
      atraso (aluguel + juros/multa), com seletor para agrupar por inquilino ou por imóvel
- [x] Layout em grade de 2 colunas (`charts-grid`), responsivo
- [x] Cores lidas das variáveis CSS do tema (`cssVar()`) — os gráficos se redesenham
      automaticamente ao trocar entre tema claro/escuro, para nunca ficar com cores do
      tema errado

### Análises — Relatórios
- [x] Aba "Relatórios" com seletor de ano
- [x] Todos os valores "recebido"/"pago" desta aba (card do ano, tabela mensal,
      comparativo anual e tabela por forma de pagamento) são **líquidos**: já descontam
      a comissão do corretor (% sobre o aluguel, quando houver) e o condomínio de cada
      pagamento, já que nenhum dos dois fica com o proprietário — mesma convenção e
      mesma função (`valorLiquidoPagamento()`) usada no Histórico. Diferente do
      Histórico, aqui só o valor líquido é mostrado (não lado a lado com o bruto), já
      que o objetivo do relatório é refletir o que de fato sobra
- [x] Tabela mensal: recebido (líquido), despesas, total em atraso, contratos no período
- [x] Totais anuais somados
- [x] Comparativo anual: tabela com todos os anos que têm dados lado a lado (recebido
      líquido, despesas, total em atraso, contratos no ano), para comparar a evolução de
      um ano para o outro sem precisar trocar o seletor repetidamente
- [x] "Total em atraso" (card do ano, tabela mensal e comparativo anual) é o valor cheio
      devido — aluguel + juros/multa — e não só o componente de juros/multa. Mesma
      convenção usada na aba Atrasos. Só o card "Total em atraso" do Dashboard continua
      somando apenas juros/multa (decisão consciente, mantido como resumo rápido)
- [x] Card "Total de descontos concedidos no ano"
- [x] Card "Total de despesas no ano" e card "Lucro líquido no ano" (recebido líquido
      menos total de despesas do ano — já líquido de corretor/condomínio duas vezes,
      então é o valor mais próximo do que efetivamente sobra)
- [x] Tabela "Pagamentos por forma no ano": quantidade e total recebido líquido por
      forma de pagamento (Dinheiro/Pix/Não informado), para o ano selecionado

### Análises — Calendário
- [x] Aba dedicada com grade mensal (estilo calendário), navegação entre meses
      (anterior/próximo/hoje)
- [x] Cada dia com vencimento ou pagamento tem o FUNDO da célula inteira colorido
      (não só pontinhos pequenos) conforme o pior status do dia — vermelho se tem algo
      atrasado, azul se tem vencimento ainda ativo, verde se só tem pago/pagamento
      recebido — muito mais fácil de escanear o mês inteiro rapidamente
- [x] Mostra também o valor total (R$) dos vencimentos daquele dia, direto na célula
      (escondido em telas pequenas para não espremer o dia), além dos pontos coloridos
      individuais (agora maiores) indicando vencimentos (azul=ativo, vermelho=atrasado,
      verde=pago) e pagamentos recebidos
- [x] Clicar em um dia mostra a lista detalhada de vencimentos e pagamentos daquela
      data abaixo do calendário

### Sistema — Auditoria
- [x] Aba dedicada com o histórico dos eventos principais do sistema — cada entrada
      mostra o quê, quem fez e quando. Tipos de evento registrados: contrato criado,
      editado, excluído, reajustado, encerrado ou reaberto; caução devolvida; dívida
      editada ou excluída; pagamento registrado; despesa criada, editada ou excluída;
      imóvel editado; usuário administrador adicionado ou removido; chave
      `COOKIE_SECRET` regenerada
- [x] Diff campo a campo: edições de contrato (imóvel, inquilino, quem recebe, corretor,
      caução), de dívida (vencimento, valores, observação), de despesa e de reajuste
      (aluguel antigo → novo) mostram uma lista "campo: valor antigo → valor novo" logo
      abaixo da descrição do evento, sempre que houver algo de fato alterado (campos
      iguais não aparecem no diff)
- [x] Filtros por ano, mês e usuário (quem fez a alteração) — os selects de ano e usuário
      são montados dinamicamente a partir dos eventos existentes, sem precisar de
      cadastro separado
- [x] Lista ordenada do mais recente para o mais antigo, mantendo os últimos 300 eventos

### Sistema — Configurações
- [x] Taxa de juros mensal (%) e taxa de multa fixa por atraso (%) — usadas no cálculo
      automático de atraso dos contratos vencidos, e também como percentual padrão dos
      campos "Juros"/"Multa" ao criar um novo contrato
- [x] Percentual padrão do corretor (%, padrão 5%) para pré-preencher novos contratos
- [x] Percentual de reajuste sugerido (%) — usado para calcular o valor de aluguel
      sugerido quando um contrato chega no aniversário de reajuste (ver seção Contratos)
- [x] Cadastro de "Pessoas (recebedores/corretores)": lista reutilizável de nomes
      (adicionar/remover), usada como seletor tanto no campo "Quem recebe" (contrato e
      pagamento) quanto no campo "Corretor" — evita digitar o nome toda vez, e um
      corretor cadastrado também pode ser escolhido como "quem recebeu" um pagamento
- [x] Conta do administrador (usuário/senha)
- [x] Segurança: botão "Gerar novo COOKIE_SECRET" (exige senha atual) — gera uma nova
      chave aleatória e reescreve `api/config.php` no servidor automaticamente (sem
      precisar editar o arquivo manualmente). Reemite a sessão do próprio administrador
      na hora, então ele continua logado; TODOS os outros usuários logados no momento
      são desconectados (precisam entrar de novo), já que a chave assina o cookie de
      login de todo mundo. Substituição feita com escrita atômica (arquivo temporário +
      `rename()`) e só acontece se o padrão exato de `COOKIE_SECRET` for encontrado no
      arquivo — nunca deixa `api/config.php` num estado quebrado
- [x] Usuários administradores: listar, adicionar, remover outros usuários com acesso
      total (não existe usuário "só leitura")
- [x] Backup do banco de dados: exportar tudo em JSON, ou restaurar a partir de um
      backup salvo (com confirmação antes de substituir os dados atuais)
- [x] Zona de perigo: excluir todos os dados (contratos, pagamentos e configuração)
      permanentemente do servidor. Exige confirmação em duas etapas (diálogo de aviso +
      digitar "EXCLUIR"), pois não pode ser desfeito. Não afeta o usuário/senha de login

### Navegação e visual
- [x] Sidebar vertical fixa de 280px substituindo a barra de abas horizontal: menu
      agrupado em blocos rotulados (Dashboard | Gestão | Financeiro | Agenda | Sistema),
      botão de recolher para 76px (só ícones, com tooltip) e estado salvo no navegador
      (`aluguelApp_sidebar`); no celular vira gaveta sobreposta com fundo escurecido
- [x] Header próprio com busca global (delega para a busca de Contratos já existente),
      botão "Atualizar dívidas", painel de notificações (espelha os alertas de vencimento
      e reajuste do Dashboard), alternância de tema e menu do usuário com atalhos e Sair
- [x] Interface reconstruída (`index.html` + `css/`) com identidade própria de SaaS:
      paleta índigo/violeta, cantos de 14px, sombras em camadas, header com efeito de
      vidro fosco (`backdrop-filter: blur`), cards com destaque ao passar o mouse,
      transições curtas com curva de easing suave (`cubic-bezier`). Nenhum framework:
      HTML, CSS e JS puros, como no resto do projeto
- [x] CSS separado por responsabilidade em `css/`: `tokens.css` (variáveis de cor,
      tipografia, espaçamento, raios, sombras e animações + tema claro/escuro),
      `components.css` (Button, Card, Input, Table, Badge, Alert, Dropdown, Modal, Tabs,
      Tooltip, Pagination, Toast, Skeleton, Loading, Empty state), `layout.css` (casca:
      sidebar, header, conteúdo, responsividade, impressão) e `screens.css` (cada tela)
- [x] `ui.js` separado do `index.js`: só comportamento visual (sidebar, gaveta, menus,
      busca do header, notificações, abas de Configurações, expandir dívidas de um
      contrato, busca/paginação em listas já renderizadas). Não tem regra de negócio,
      cálculo nem chamada de API — tudo isso continua exclusivamente no `index.js`
- [x] Sistema de ícones em SVG (sprite `<symbol>` inline em `index.html`, sem depender de
      biblioteca externa) — logo, abas de navegação, cards de estatística, botões de
      ação (pagar, editar, excluir, reajustar, anexo, exportar/importar), alternância de
      tema (sol/lua) e campo de busca. Ícones usam `stroke="currentColor"`, herdando
      automaticamente a cor do tema/contexto sem CSS extra por ícone
- [x] Alternância dark/light, preferência salva no navegador; detecção automática do
      tema do sistema operacional na primeira visita (antes de qualquer escolha manual),
      acompanhando mudanças em tempo real enquanto não houver preferência salva
- [x] Sistema de design consolidado (`css/tokens.css`): escala tipográfica única
      (`--text-2xs` a `--text-3xl`, piso de 11px — nada no site fica menor que isso, fonte
      Inter com fallback para a fonte do sistema) e escala de espaçamento em múltiplos de
      8 (`--space-1` a `--space-9`), além de raios (padrão 14px), sombras, durações e
      curvas de animação — todos como variáveis CSS reutilizadas em cada regra, nenhum
      valor solto digitado num componente. Números importantes usam `tabular-nums` para
      alinhar coluna a coluna. ATENÇÃO: `--accent`, `--success`, `--danger`, `--warn`,
      `--border`, `--text`, `--text-dim` e `--text-faint` são lidos pelo JS dos gráficos
      (`cssVar()`); os três primeiros precisam ser hex de 6 dígitos (o canvas concatena
      transparência no fim da cor)
- [x] Corrigido um bug de espaçamento "duplicado": `.card` tinha `margin-bottom` próprio
      E os containers que o envolvem (`.cards-grid`, `.charts-grid`, `.cards-list`) já
      tinham `gap` — as duas coisas juntas faziam o espaço entre linhas de cards ficar
      maior (e diferente) do espaço entre colunas, na mesma tela. Agora só o container
      define o espaçamento quando existe um; cards "soltos" (não dentro de grid/lista,
      como os cards de Configurações) continuam com seu próprio espaçamento
- [x] Estilos inline soltos (`style="..."`) nos templates de contrato/relatório/
      histórico/calendário foram substituídos por classes utilitárias reaproveitáveis
      (`.text-danger`, `.is-current`, `.bg-accent/.bg-danger/.bg-success`, `.mt-sm`,
      `.mt-md`, `.panel-header-tight`) — mesma aparência, mas agora nenhuma cor ou
      espaçamento é digitado direto num elemento por fora do sistema de design

### Usabilidade
- [x] Atalhos de teclado: `N` abre "Novo contrato", `/` foca a busca de contratos,
      `Esc` fecha qualquer modal aberto. Não interferem enquanto o foco está em um
      campo de formulário

### Exportação / Importação
- [x] Exportar CSV — uma linha por dívida, incluindo número do contrato (respeita
      filtros ativos)
- [x] Exportar PDF (via canvas + impressão do navegador) — mesma granularidade do CSV
- [x] Importar contratos via CSV (mesmo formato do exportado) — cada linha vira um
      contrato novo com uma única dívida
- [x] Exportar backup completo (JSON) com contratos (já no formato com dívidas) +
      configuração + pessoas + imóveis + despesas
- [x] Importar/restaurar backup completo (JSON), com confirmação antes de substituir —
      se o backup estiver num formato antigo (contratos sem dívidas, sem número, sem
      `pessoas`/`imoveis`/`despesas`), é migrado automaticamente também

### Backend / infraestrutura
- [x] Todos os dados gravados em arquivos no servidor (`data/dados.json`, `data/auth.json`)
- [x] Gravação protegida com `flock` (evita corrupção em acessos simultâneos)
- [x] Pasta `data/` bloqueada para acesso direto via `.htaccess`
- [x] Testado localmente com PHP 8.5 (`php -S`) — login, troca de senha, geração de
      COOKIE_SECRET, leitura/gravação de dados e validações de erro (400/401) todos
      conferidos via curl

### Documentação
- [x] `README.md` com foco em iniciantes: credenciais padrão em destaque, passo a passo
      de instalação/hospedagem, FAQ, e detalhes técnicos organizados no final
- [x] `etapas.txt` reorganizado seguindo os 5 grupos de navegação do sistema, com mapa
      das telas no topo para orientação rápida

## ⚠️ PENDENTE / ATENÇÃO ANTES DE PRODUÇÃO
- [x] Trocar `COOKIE_SECRET` em `api/config.php` por um valor aleatório próprio — não
      precisa mais editar o arquivo manualmente, tem um botão em Configurações >
      Segurança ("Gerar novo COOKIE_SECRET") que faz isso pelo próprio site
- [ ] Testar em hospedagem real com Apache (confirmar que `.htaccess` bloqueia `data/`
      pela URL — isso não é validado pelo servidor embutido do PHP usado em testes locais)
- [ ] Trocar a senha padrão (`admin`/`12345678`) assim que possível após o primeiro deploy
- [x] Revisão de segurança de todos os arquivos em `api/`: confirmado que cookies são
      HMAC-assinados e comparados com `hash_equals` (à prova de timing attack), senhas
      sempre com `password_hash`/`password_verify`, upload de anexo valida extensão E
      tipo MIME real do conteúdo (não só a extensão), nomes de arquivo protegidos contra
      path traversal, `data/`/`contratos/` bloqueados por `.htaccess`, escritas em disco
      sempre atômicas (arquivo temporário + `rename()`, ou `flock`). Corrigido nessa
      revisão: login sem limite de tentativas (bruta força), senha mínima de 4
      caracteres (fraca), e adicionar/remover outro administrador sem reconfirmar senha
      — ver "Login e conta" acima
- [ ] Rate limiting do login é só por IP (sem CAPTCHA, sem 2FA) — pode falso-positivar
      várias pessoas atrás do mesmo IP/NAT (ex: escritório) se todas errarem a senha, e
      não impede um atacante com muitos IPs diferentes. Suficiente para o caso de uso
      deste sistema (uso interno, poucos usuários), mas não é proteção de nível
      enterprise

## 🚫 NÃO IMPLEMENTADO (decisão consciente, não é pendência)
- Notificações por e-mail de vencimentos — decidiu-se não implementar por exigir SMTP
  configurado na hospedagem, sem garantia de entrega. O alerta de vencimentos existe
  só dentro do site (Dashboard).

## IDEIAS PARA O FUTURO (não solicitadas ainda)

1. Anexar comprovantes de pagamento (um arquivo por pagamento registrado, além do
   contrato assinado já anexável ao contrato)
2. Campo de telefone/e-mail do inquilino, com atalho para WhatsApp direto na lista
   de contratos
3. Geração automática de recibo em PDF por pagamento individual (hoje o PDF é só o
   relatório geral de contratos)

7. Notificação dentro do navegador (Web Push) para avisos de vencimento, sem precisar
   de e-mail/SMTP

11. Modo multi-imóvel/multi-proprietário — separar contratos por carteira, útil para
    quem administra imóveis de terceiros
12. Backup automático agendado (ex: gerar e salvar um `.json` de backup toda semana
    no próprio servidor, sem precisar acionar manualmente)
13. Progressive Web App (PWA) — permitir "instalar" o sistema no celular com ícone
    próprio e uso básico offline
14. Papéis de permissão distintos entre usuários administradores (ex: um usuário só de
    visualização/relatórios, sem poder excluir dados) — hoje todos os usuários têm
    acesso total e igual
18. Índice de reajuste (IGP-M/IPCA) consultado automaticamente — hoje o percentual
    sugerido é digitado manualmente em Configurações, já que o sistema não tem acesso à
    internet (nenhuma dependência externa é uma decisão de arquitetura deste projeto)
